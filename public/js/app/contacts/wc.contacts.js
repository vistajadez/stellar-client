(function (window, angular) {
    "use strict";

    /**
     *
     * @description
     * Module for contacts-related functionality.
     */
    angular.module('wc.contacts', [
        // Controllers

        // Directives

        // Services
        'wc.contacts.service'
    ]);
})(window, window.angular);