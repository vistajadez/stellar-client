(function (window, angular) {
    "use strict";

    /**
     *
     * @description
     * Main module for listing contacts that are due.
     */
    angular.module('wc.duecontacts', [
        // Controllers

        // Directives
        'wc.duecontacts.directive'

    ]);
})(window, window.angular);
