(function (window, angular) {
    "use strict";

    /**
     *
     * @description
     * Main module for pulling in dependencies for the dashboard.
     */
    angular.module('wc.dashboard', [
        // Controllers
        'wc.dashboard.controller'
    ]);
})(window, window.angular);
