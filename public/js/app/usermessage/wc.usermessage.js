(function (window, angular) {
    "use strict";

    /**
     *
     * @description
     * Module for messaging the user. Makes use of Angular-Growl library for growl-style messaging.
     */
    angular.module('wc.usermessage', [
        // Controllers

        // Directives

        // Services
        'wc.usermessage.service'
    ]);
})(window, window.angular);