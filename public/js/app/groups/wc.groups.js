(function (window, angular) {
    "use strict";

    /**
     *
     * @description
     * Module for groups-related functionality.
     */
    angular.module('wc.groups', [
        // Controllers

        // Directives

        // Services
        'wc.groups.service'
    ]);
})(window, window.angular);