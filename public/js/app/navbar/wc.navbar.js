(function (window, angular) {
    "use strict";

    /**
     *
     * @description
     * Main module for pulling in dependencies for the navbar.
     */
    angular.module('wc.navbar', [
        // Controllers
        'wc.navbar.controller'
    ]);
})(window, window.angular);
