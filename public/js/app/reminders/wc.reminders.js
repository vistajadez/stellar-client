(function (window, angular) {
    "use strict";

    /**
     *
     * @description
     * Displays a ist of reminders (i.e. birthdays, anniversaries).
     */
    angular.module('wc.reminders', [
        // Controllers

        // Directives
        'wc.reminders.directive'

    ]);
})(window, window.angular);