(function (window, angular) {
    "use strict";

    /**
     *
     * @description
     * Module for authentication functionality.
     */
    angular.module('wc.auth', [
        // Controllers

        // Directives

        // Services
        'wc.auth.service'
    ]);
})(window, window.angular);